server {
    listen 80;

    location /dashboard {
        root   /usr/share/nginx/html;
        index  index.html;
        try_files $uri /index.html;
    }

    location /dashboard/ {
        root   /usr/share/nginx/html;
        index  index.html;
        try_files $uri /index.html;
    }
}
